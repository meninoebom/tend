[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm exec next start -p ${PORT:-3000}"
healthcheckPath = "/login"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
